---
phase: 01-vite-foundation
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - index.html
autonomous: true
requirements:
  - DEP-03
  - DEP-04
  - DEP-05
  - DEP-06

must_haves:
  truths:
    - '`index.html` has exactly one `<script type="module">` tag pointing to `/src/main.js`'
    - "No CDN `<script>` or `<link>` tags for d3, vis, box2d, or seedrandom remain in `index.html`"
    - 'The `<link rel="stylesheet" href="...vis...">` CDN tag is removed from `<head>`'
    - "`styles.css` local link remains in `<head>` — untouched"
  artifacts:
    - path: "index.html"
      provides: "Single module entry point replacing all CDN/vendored script tags"
      contains: '<script type="module" src="/src/main.js">'
  key_links:
    - from: "index.html"
      to: "src/main.js"
      via: '<script type="module">'
      pattern: 'type="module".*src.*main\.js'
---

<objective>
Remove all CDN and vendored `<script>` tags from `index.html` and add the single `<script type="module" src="/src/main.js">` entry point.

Purpose: This is the HTML-side of the migration. After this change, Vite owns the entire module graph from `src/main.js` downward. The browser no longer loads any scripts directly — everything goes through Vite.

Output: Modified `index.html` with 5 tags removed and 1 added. The `Watch Leader` button camera timing fix (disabled attribute) is handled in Plan 04 alongside the `cw_runningInterval` fix — both relate to the strict-mode/timing issue and belong together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vite-foundation/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Exact tags to remove and add. Extracted from index.html direct inspection. -->

Current `index.html` script/link block to remove (lines 7, 305-309):

Line 7 in `<head>` — remove:

```html
<link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/vis/4.20.0/vis.min.css"
/>
```

Lines 305-309 at bottom of `<body>` — remove all five:

```html
<script src="lib/seedrandom.js"></script>
<script src="lib/box2d.js"></script>
<script src="https://d3js.org/d3.v3.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/vis/4.20.0/vis.min.js"></script>
<script src="src/app.js"></script>
```

Replace the five script lines with one line:

```html
<script type="module" src="/src/main.js"></script>
```

Keep these untouched:

- Line 8: `<link rel="stylesheet" href="styles.css">` — local CSS, stays
- All DOM elements (canvas, divs, inputs, the PayPal form, everything)
- Line 189: `<input type="button" value="Watch Leader" onclick="cw_setCameraTarget(-1)" />` — Plan 04 adds `disabled` here
- Line 300: `<div name="healthbar" class="healthbar" onclick="cw_setCameraTarget(this.car_index)">` — Plan 04 handles onclick timing here
  </interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Remove CDN tags and add module entry point</name>
  <files>index.html</files>
  <read_first>
    - `index.html` — read the entire file before editing; confirm current line numbers match expected locations
    - `src/main.js` — confirm it exists (created in Plan 02) before referencing it in HTML
  </read_first>
  <action>
Edit `index.html` to make exactly these changes:

**Change 1 — Remove from `<head>`:**
Remove the line:

```html
<link
  rel="stylesheet"
  href="https://cdnjs.cloudflare.com/ajax/libs/vis/4.20.0/vis.min.css"
/>
```

The `<link rel="stylesheet" href="styles.css">` line immediately below it MUST remain.

**Change 2 — Replace the five script tags at the bottom of `<body>`:**
Remove these five lines (currently near the bottom of `<body>`, just before `</body>`):

```html
<script src="lib/seedrandom.js"></script>
<script src="lib/box2d.js"></script>
<script src="https://d3js.org/d3.v3.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/vis/4.20.0/vis.min.js"></script>
<script src="src/app.js"></script>
```

Replace them with this single line:

```html
<script type="module" src="/src/main.js"></script>
```

No other changes to `index.html` in this plan. The camera button `disabled` attribute is Plan 04's responsibility.

After editing, do a quick sanity check: the file should still contain `id="mainbox"`, `id="generation"`, `id="health"`, `id="cars"`, and the `Watch Leader` input button — these are critical DOM IDs that `src/app.js` references.
</action>
<verify>
<automated>grep -c "cdnjs\|d3js\|vis\.min\|vis\.v3\|d3\.v3" /Users/kellen/Projects/HTML5_Genetic_Cars/index.html | grep -q "^0$" && echo "PASS: no CDN links" || echo "FAIL: CDN links remain"</automated>
    <automated>grep -c 'src="lib/seedrandom\.js"\|src="lib/box2d\.js"\|src="src/app\.js"' /Users/kellen/Projects/HTML5_Genetic_Cars/index.html | grep -q "^0$" && echo "PASS: vendored script tags removed" || echo "FAIL: old script tags remain"</automated>
<automated>grep -q 'type="module"' /Users/kellen/Projects/HTML5_Genetic_Cars/index.html && echo "PASS: module script tag present" || echo "FAIL: module script tag missing"</automated>
<automated>grep -q 'src="/src/main\.js"' /Users/kellen/Projects/HTML5_Genetic_Cars/index.html && echo "PASS: main.js entry point" || echo "FAIL: main.js entry missing"</automated>
<automated>grep -q 'href="styles\.css"' /Users/kellen/Projects/HTML5_Genetic_Cars/index.html && echo "PASS: styles.css kept" || echo "FAIL: styles.css removed by mistake"</automated>
<automated>grep -q 'id="mainbox"' /Users/kellen/Projects/HTML5_Genetic_Cars/index.html && echo "PASS: mainbox canvas intact" || echo "FAIL: mainbox removed"</automated>
<automated>grep -q 'Watch Leader' /Users/kellen/Projects/HTML5_Genetic_Cars/index.html && echo "PASS: Watch Leader button intact" || echo "FAIL: Watch Leader button removed"</automated>
</verify>
<acceptance_criteria> - `grep -c "cdnjs\|d3js\|vis\.min" index.html` outputs `0` - `grep -c 'src="lib/seedrandom' index.html` outputs `0` - `grep -c 'src="lib/box2d' index.html` outputs `0` - `grep -c 'src="src/app' index.html` outputs `0` - `grep -q 'type="module" src="/src/main.js"' index.html` exits 0 - `grep -q 'href="styles.css"' index.html` exits 0 (local CSS kept) - `grep -q 'id="mainbox"' index.html` exits 0 - `grep -q 'id="generation"' index.html` exits 0 - `grep -q 'id="health"' index.html` exits 0 - `grep -q 'Watch Leader' index.html` exits 0 - Total `<script` tags in file: exactly 1 (the new module entry)
</acceptance_criteria>
<done>`index.html` has no CDN or vendored `<script>`/`<link>` tags. A single `<script type="module" src="/src/main.js">` replaces the five removed tags. All DOM structure is intact.</done>
</task>

</tasks>

<verification>
After task completes:

```bash
# No CDN or vendored script/link tags remain
echo "CDN check:"
grep -E "cdnjs|d3js|vis\.min|seedrandom|box2d|src/app" index.html | wc -l

# Exactly one script tag remains
echo "Script tag count:"
grep -c "<script" index.html

# Module entry point is correct
echo "Module entry:"
grep 'type="module"' index.html

# styles.css local link intact
echo "styles.css:"
grep 'styles.css' index.html

# Critical DOM IDs intact
echo "DOM IDs:"
grep -E 'id="mainbox"|id="generation"|id="health"|id="cars"' index.html | wc -l
```

Expected: CDN check = 0, script tag count = 1, module entry present, styles.css present, DOM IDs count = 4+.
</verification>

<success_criteria>

- `index.html` has exactly 1 `<script>` tag: `<script type="module" src="/src/main.js">`
- Zero CDN `<script>` or `<link>` tags (d3, vis, box2d, seedrandom all removed)
- `<link rel="stylesheet" href="styles.css">` is still present
- All original DOM structure intact (canvas IDs, button IDs, form, etc.)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-vite-foundation/01-03-SUMMARY.md` with:
- Confirmation of tags removed (list the exact lines deleted)
- Confirmation of tag added
- Output of `grep -c "<script" index.html` (should be 1)
- Output of `grep -c "cdnjs\|d3js\|vis\.min" index.html` (should be 0)
</output>
